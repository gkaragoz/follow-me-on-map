name: server
description: Follow Me On Map - Server
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.1
  shelf_static: ^1.1.3
  web_socket_channel: ^3.0.2
  sqlite3: ^3.0.0
  args: ^2.6.0
  shared:
    path: ../shared
